Commiteá los cambios pendientes y hacé el build para distribuir.

Los archivos modificados en esta sesión son:
- src/handlers/clientHandler.js
- src/config/messages.js
- index.js

Paso 1 — commit:
git add src/handlers/clientHandler.js src/config/messages.js index.js
git commit -m "fix: historial vuelve al menu, quitar pregunta repetimos, manejo detached frame"

Paso 2 — build:
npm run build

Confirmame cuando terminen los dos pasos.
