Leé el archivo `prompts/implementacion-clientes-calendar.md` y ejecutá todo lo que indica.

El archivo tiene dos partes:
1. Contexto del proyecto — leelo para entender el stack y la estructura
2. Implementación — hacé todos los cambios que describe, en orden

Antes de arrancar, commiteá el cambio pendiente en tray/setup-window.js:
git add tray/setup-window.js
git commit -m "fix: limpiar listeners IPC huerfanos al cerrar setup-window"

Luego procedé con la implementación completa.
